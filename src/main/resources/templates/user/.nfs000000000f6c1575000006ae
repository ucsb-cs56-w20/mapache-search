<!DOCTYPE HTML>
<html xmlns:th="http://www.thymeleaf.org" lang="en">

<head>
  <div th:replace="bootstrap/title.html"></div>
  <div th:replace="bootstrap/bootstrap_head.html"></div>
</head>

<body>
  <div class="container">
    <div th:replace="fragments/bootstrap_nav_header.html"></div>

    <h1>? ? ?</h1>

    <form action="#" th:action="@{/instructor/random_student_results}" th:object="${AppUser}" method="get">
       <input type="submit" class="btn btn-primary" value="Regenerate">
    </form>
    
    <div class="container" style="margin-top: 30px">
        <h5>Number of API use made today</h5>
        <p th:text="${api_uses} + '/' + ${max_uses}"> </p>
    </div>

    <div class="container" style="margin-top: 30px">
      <h5>Number of API use in total</h5>
      <p th:text="${api_uses} + '/' + ${time}"> </p>
  </div>
    
    <p>Upvote History</p>
    <table class = "table">
				<tbody>
					<div th:if=" ${userVotes.size()} == 0">
						<h4 class = "text-center"> No upvoted links</h4>
					</div>
					<div th:if=" ${userVotes.size()} != 0">
					  <tr th:each="f: ${userVotes}"> 
						  <td><a th:href="${f.getResult().getUrl()}" th:text="${f.getResult().getUrl()}"></a></td>
					  </tr>
					</div>
				</tbody>
    </table>
    <div th:replace="bootstrap/bootstrap_footer.html"></div>
  </div>
  <div th:replace="bootstrap/bootstrap_scripts.html"></div>
</body>

</html>